# PR наверх: черновик шагов (progress-draft)

**Статус:** в работе с 24.09.2026. Пункт 2 плана `plan/done/port-fork-to-9.6.md`.

Невлитое у нас одним коммитом `58174b1` (22 файла): черновик шагов, досылка хвоста
озвучки (`tts-parts`), диагностика ответа сервера загрузки, зеркало рантайма, оснастка
проверок (типы, изоляция тестов). Наверх — **по отдельности**, первым черновик шагов.

- [x] 1. Выделить черновик шагов от `origin/main` (апстрим 2026.9.6) в ветку `pr/progress-draft`:
  `src/progress-draft.ts` + тесты + врезка в `inbound.ts`/настройки — только то, без чего он не работает.
- [x] 2. Тесты и сборка апстрима зелёные на ветке; поведение совпадает с продом.
- [x] 3. Ревью агентом `vk-pr-reviewer`, TruffleHog.
- [x] 4. Описание PR (по-английски, как прошлые #13/#14/#16): что, зачем, как проверено, скриншот.
- [x] 5. Push в `Shagrat2/openclaw-vk`, PR в `pfrankov/openclaw-vk`.
- [ ] 6. Следом — диагностика ответа сервера загрузки; затем `tts-parts`.

## Найдено при выделении (24.09)

- [x] 🔴 **Прод потерял исправления апстрима из #16** (возвращены `d550b41`, раскатано 24.09) при переносе 22.09: в нашем `inbound.ts`
  нет `collectVkOwnMedia` (голосовое из поста со стены не скачивается), `isQuoteVisible`
  (режим `allowlist` не прячет чужую цитату) и лога «forwards hidden by contextVisibility».
  В раскатанном dist — 0 вхождений. Вернуть в `main` из `origin/main`, тесты, раскатка.
- [ ] Ошибка апстрима: в ack-гейт передаётся `requireMention`, а у гейта поле `shouldBypassMention`
  (видно в tsc). У нас исправлено; кандидат в отдельный маленький PR.
- [ ] В `~/.openclaw/openclaw.json` открытым текстом токены VK и MAX и логин-пароль прокси —
  вопреки схеме keychain (VK-форк ждёт string — известно; MAX и прокси — проверить SecretRef).

## Ревью vk-pr-reviewer 24.09 — `workspace/reviews/vk/progress-draft-6d5ede4.md` в репо openclaw

Вердикт: отправлять нельзя, 4×P1 + 5×P2. Логика черновика та же, что на проде, — **P1, скорее
всего, живут и на проде**. Чинить сначала в `main` (прод), тесты — на НАСТОЯЩИХ функциях ядра,
а не на моке (мок «самый длинный текст» прятал P1-1), потом перенести в ветку PR.

- [x] P1-1 (прод `main`, раскатано 24.09) пустой финал удаляет черновик с ответом (ядро 9.6: `selectLongerFinalText` → undefined для `""`).
- [x] P1-2 (прод `main`, раскатано 24.09) блок, не влезший в черновик, уходит отдельно, следующий перезаписывает черновик — текст блока 1 теряется, порядок ломается.
- [x] P1-3 (прод `main`, раскатано 24.09) ответ, вписанный в черновик, теряет вложения из markdown-ссылок (`![…](url)`, `[файл](/path)`).
- [x] P1-4 (прод `main`, раскатано 24.09) сбой хвоста ответа проглатывается, реакция «готово» при недоставке.
- [x] P2 черновик висит, если ход без финала (прод `04513e7` + `af08a8a`) (NO_REPLY, `message`, прерывание) — убирать в `finally`, как Telegram.
- [ ] P2 цитата в группе и на кнопку (`replyTo` при первой отправке); снимать клавиатуру после команды с кнопки.
- [ ] P2 доки: `progress.toolProgress: true` обязателен на 9.6; «core does not add the label» неверно; «every row is tested» неверно; подпуть `channel-outbound`.
- [ ] P2 CHANGELOG/SCENARIOS по AGENTS.md апстрима (тег, «Кому важно», сценарии; строка про старые значения `streaming`).
- [ ] Можно после: `runtime.log` → `vkDiag`; замок на запись черновика.

## Прод 24.09 вечер

Ветка `fix/prod-regressions` (7 коммитов + `af08a8a` по находке второго ревью
`workspace/reviews/vk/prod-regressions-04513e7.md` в репо openclaw) влита fast-forward в `main`,
раскатана `deploy-vk-plugin.sh`, `main` запушен в `shagrat`. 917 тестов, типы против 9.6 чистые.
После раскатки смотреть в ленте: `draft kept as answer`, `block overflows draft`,
`answer tail failed`, `vk final reply failed`, `step-progress draft removed`.
Дальше — перенос в ветку PR (агент), повторное ревью, PR.

## Ветка PR 24.09 вечер

Коммиты `51329de` (P1 + уборка + сброс при перерисовке, тест на настоящем ядре в `test:sdk`),
`5100be3` (цитата replyTo, клавиатура после кнопки — обычным путём), `536bf14` (vkDiag),
`eb5fe80` (CHANGELOG v2026.9.7, README, SCENARIOS). Проверки на ядрах 8.1 и 9.6 зелёные.
Повторное ревью — перед отправкой.

- [ ] После PR принести в прод `main`: `replyTo` черновика (группа, кнопка), `&& !payloadCommand`
  при вписывании финала, трассировка через `vkDiag` (строки `step-progress …` будут видны только
  при `diagnostics.level` ≥ redacted — у нас `full`, ок).

## Отправлено 24.09

PR [pfrankov/openclaw-vk#18](https://github.com/pfrankov/openclaw-vk/pull/18), ветка
`Shagrat2:pr/progress-draft` (6 коммитов, последний `40f23fe`). Повторное ревью —
`workspace/reviews/vk/progress-draft-eb5fe80.md` (репо openclaw): блокеров нет, его P2 про брошенный
черновик закрыт и в PR, и на проде (`4b6dd13`, раскатано). Вживую не проверено: цитата и правка в
групповой беседе (на проде только личка).
